import _property from "lodash/property";

const app = _property("App");
const category = _property("Category");
const rating = _property("Rating");
const reviews = _property("Reviews");
const size = _property("Size");
const installs = _property("Installs");
const type = _property("Type");
const price = _property("Price");
const contentRating = _property("Content Rating");
const genres = _property("Genres");
const lastUpdated = _property("Last Updated");
const currentVersion = _property("Current Ver");
const androidVersion = _property("4.0.3 and up");

export const appReader = {
  app,
  category,
  rating,
  reviews,
  size,
  installs,
  type,
  price,
  contentRating,
  genres,
  lastUpdated,
  currentVersion,
  androidVersion,
};
